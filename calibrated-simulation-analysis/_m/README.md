# Generated outputs

This directory is populated by the scripts in `../_h/`. Expected subdirectories
are `config/`, `raw/`, `calibration/`, `evaluation/`, `figures/`, `observed/`,
and `logs/`. Large per-simulation and per-VMR outputs should not be committed
without an explicit repository data-management decision.
